---
const { class: className = "" }: { class?: string } = Astro.props;
---

<p
  class:list={[
    "font-mono text-xs uppercase tracking-wide text-signal flex items-center gap-2",
    className,
  ]}
>
  <slot />
</p>
